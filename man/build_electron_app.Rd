% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build_electron_app}
\alias{build_electron_app}
\title{Build Electron Application}
\usage{
build_electron_app(
  app_dir,
  output_dir,
  app_name = NULL,
  app_type = "r-shiny",
  runtime_strategy = "shinylive",
  sign = FALSE,
  platform = NULL,
  arch = NULL,
  icon = NULL,
  config = NULL,
  overwrite = FALSE,
  verbose = TRUE,
  prune_r_library = NULL,
  prune_r_runtime = NULL
)
}
\arguments{
\item{app_dir}{Character string. Path to the converted Shiny/shinylive application.}

\item{output_dir}{Character string. Path where the built Electron app will be saved.}

\item{app_name}{Character string. Name of the application. If NULL, uses the base name of app_dir.}

\item{app_type}{Character string. Language of the Shiny app: \code{"r-shiny"}
(default) or \code{"py-shiny"}. Unlike \code{export()}, this function does \strong{not}
autodetect the language from source files -- the default \code{"r-shiny"} is
used when \code{app_type} is not supplied. Supply \code{"py-shiny"} explicitly for
Python Shiny applications. The legacy values \code{"r-shinylive"} /
\code{"py-shinylive"} are accepted with a deprecation warning and translate to
the canonical language plus \code{runtime_strategy = "shinylive"}.}

\item{runtime_strategy}{Character string. Runtime strategy: \code{"shinylive"},
\code{"bundled"}, \code{"system"}, \code{"auto-download"}, or \code{"container"}. Default
\code{"shinylive"}.}

\item{sign}{Logical. Whether to enable code signing for the built application.
Default is FALSE.}

\item{platform}{Character vector. Target platforms: "win", "mac", "linux". If NULL, builds for current platform.}

\item{arch}{Character vector. Target architectures: "x64", "arm64". If NULL, uses current architecture.}

\item{icon}{Character string. Path to application icon file. Platform-specific format required.}

\item{config}{List. Configuration from _shinyelectron.yml file (optional). Used for
template variables like window dimensions, port, and app version.}

\item{overwrite}{Logical. Whether to overwrite existing output directory. Default is FALSE.}

\item{verbose}{Logical. Whether to display detailed progress information. Default is TRUE.}

\item{prune_r_library}{Logical or NULL. Prune build-only files (include/, tests/, examples/) from the bundled R package library. NULL uses the config \code{optimize: r_library} value, defaulting to TRUE.}

\item{prune_r_runtime}{Logical or NULL. Prune doc/, tests/ and include/ from the portable R distribution. NULL uses the config \code{optimize: r_runtime} value, defaulting to TRUE.}
}
\value{
Character string. Path to the built Electron application directory.
}
\description{
Builds a distributable Electron application from a converted Shiny app.
Creates platform-specific installers and executables.
}
\section{Details}{

This function creates a complete Electron application by:
\itemize{
\item Setting up the Electron project structure
\item Copying application files and templates
\item Installing npm dependencies
\item Building platform-specific distributables
}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Build Electron app for current platform
build_electron_app(
  app_dir = "path/to/shinylive/app",
  output_dir = "path/to/electron/build",
  app_name = "My Shiny App",
  app_type = "r-shiny"
)

# Build for multiple platforms
build_electron_app(
  app_dir = "path/to/app",
  output_dir = "path/to/build",
  app_name = "My App",
  app_type = "r-shiny",
  platform = c("win", "mac", "linux")
)
\dontshow{\}) # examplesIf}
}
