% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-runtime.R
\name{embed_r_runtime}
\alias{embed_r_runtime}
\title{Embed a portable R runtime into a bundled Electron build}
\usage{
embed_r_runtime(
  output_dir,
  packages,
  repos,
  version,
  platform,
  arch,
  verbose = TRUE,
  prune_r_library = TRUE,
  prune_r_runtime = TRUE,
  local_packages = character(0)
)
}
\arguments{
\item{output_dir}{Character. The Electron app output directory.}

\item{packages}{Character vector. DIRECT R package names (may be empty/NULL).}

\item{repos}{Character vector. CRAN-like repository URLs.}

\item{version}{Character. Resolved R version (non-NULL from callers).}

\item{platform}{Character scalar. Target platform ("win"/"mac"/"linux").}

\item{arch}{Character scalar. Target architecture ("x64"/"arm64").}

\item{verbose}{Logical. Whether to display progress.}

\item{prune_r_library}{Logical. Prune build-only files (include/, tests/, examples/) from the bundled package library.}

\item{prune_r_runtime}{Logical. Prune doc/, tests/ and include/ from the portable R distribution.}

\item{local_packages}{Character vector. Paths to local R package source directories or archives to install into the bundled library after the repository packages.}
}
\value{
Invisibly, the path to the embedded \code{runtime/R} directory.
}
\description{
Behavior-preserving extraction of the R bundled-embedding block from
\code{\link[=build_electron_app]{build_electron_app()}}. ALWAYS installs + copies the interpreter (and resolves
symlinks) so the shared \code{runtime/R} path exists for suite-wide bundled
detection; only the package install is gated on a non-empty \code{packages} set.
\code{packages} is the DIRECT set (as stored in \code{dependencies.json}); the recursive
dependency closure and the \code{pre_installed} setdiff are resolved here, against
the freshly-created \code{runtime/R/library}.
}
\keyword{internal}
